{
  "name": "smart-telegram-userbot",
  "description": "Your Smart Telegram Userbot",
  "repository": "https://github.com/AashooSharma/Smart-Telegram-Userbot.git",
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API hash",
      "required": true
    },
    "SESSION_STRING": {
      "description": "Telegram user session string",
      "required": true
    },
    "OWNER_ID": {
      "description": "Telegram user ID of the owner",
      "required": true,
      "value": "1217850333"
    },
    "SUDO_USERS": {
      "description": "Comma-separated list of sudo user IDs",
      "required": true,
      "value": "1217850333"
    }
  },
  "stack": "container",
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-apt"
    },
    {
      "url": "https://github.com/heroku/heroku-buildpack-python"
    }
  ],
  "scripts": {
    "postdeploy": "python setup.py"
  },
  "addons": []
}
